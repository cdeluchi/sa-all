var x = 25;
var doublex = [];

function timesTwo(number) {
    return number * 2;
    for (var i = 0; i < number; i++);
    number[{ y: doublex }];
}
var numbers = [x, doublex];
timesTwo(x[doublex]);
